export interface ColorTheme {
  background: string;
  primary: string;
  secondary: string;
  accent: string;
}

export const DEFAULT_THEME: ColorTheme = {
  background: '#000000',
  primary: '#ffffff',
  secondary: '#a855f7',
  accent: '#3b82f6'
};